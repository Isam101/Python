# Exercise Calculate the Area of Rectangle 
# To calculate the Area of a Rectangle we need to get the length and width of the rectangle 
# Prompt the user to enter the measurments 

Width = int ( input("What is the width of the Rectangle: "))

Length = int (input("What is the length of the Rectangle: "))

Area = Length * Width

print(f" The Area of the rectangle is {Area}")


# Since area might contain decimals aswell float is the best method to use 
# Exercise Calculate the Area of Rectangle 
# To calculate the Area of a Rectangle we need to get the length and width of the rectangle 
# Prompt the user to enter the measurments 

Width = float ( input("What is the width of the Rectangle: "))

Length = float (input("What is the length of the Rectangle: "))

Area = Length * Width

print(f" The Area of the rectangle is {Area} cm^2 ")











