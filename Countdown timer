# FOR LOOPS
# This loop counts up from 1 to the chosen number, pausing 1 second between each count.

import time

# Ask the user how many seconds to count
your_time_is_up = int(input("Please enter the time in seconds: "))

# Loop from 1 up to the entered number (including the number)
for x in range(1, your_time_is_up + 1):
    print(x)
    time.sleep(1)  # Pause for 1 second

print("Time's Up!")
