# WHILE LOOP – INPUT VALIDATION
# This program calculates the final amount using the compound interest formula:
# A = P * (1 + r/100)^t
# It validates that principal, rate, and time must all be greater than zero.

principle_amount = 0
rate = 0
time = 0

# Validate principal amount
while principle_amount <= 0:
    principle_amount = float(input("Please enter the principal amount (must be > 0): "))
    if principle_amount <= 0:
        print("The principal amount cannot be less than or equal to zero.")

# Validate interest rate
while rate <= 0:
    rate = float(input("Please enter the interest rate (must be > 0): "))
    if rate <= 0:
        print("The interest rate cannot be less than or equal to zero.")

# Validate time in years
while time <= 0:
    time = float(input("Please enter the time in years (must be > 0): "))
    if time <= 0:
        print("Time cannot be less than or equal to zero.")

# Calculate the final amount using compound interest formula
Final_Amount = principle_amount * pow((1 + (rate / 100)), time)

# Display result with formatting
print(f"Your final amount after {time} year(s) at an interest rate of {rate}% is: ${Final_Amount:,.2f}")
